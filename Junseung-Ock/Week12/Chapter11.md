# 뉴스 피드 시스템 설계

---

## 문제 이해 및 설계 범위 확정

- 웹/앱 지원 여부
- 주요 기능 (피드 올리기, 피드 조회)
- 피드 순서
- 최대 친구 수
- 트래픽 규모
- 이미지나 비디오 피드 추가 가능 여부

## 개략적 설계안 제시

### 피드 발행

사용자가 스토리를 포스팅하면 해당 데이터를 캐시와 데이터베이스에 기록

새 포스팅은 친구의 뉴스 피드에도 전송

- 사용자 : 모바일 앱이나 브라우저에서 새 포스팅을 올리는 주체
- 로드밸런서 : 트래픽을 웹 서버들로 분산
- 웹 서버 : HTTP 요청을 내부 서비스로 중계하는 역할
- 포스팅 저장 서비스 : 새 포스팅을 데이터베이스와 캐시에 저장
- 포스팅 전송 서비스 : 새 포스팅을 친구의 뉴스 피드에서 볼 수 있도록 함
- 알림 서비스 : 친구들에게 새 포스팅이 올라왔음을 알리거나, 푸시 알림을 보내는 역할

### 뉴스 피드 생성

모든 친구의 포스팅을 시간 흐름 역순으로 모아 만듦

- 사용자 : 뉴스 피드를 읽는 주체
- 로드밸런서 : 트래픽을 웹 서버들로 분산
- 웹 서버 : 트래픽을 뉴스 피드 서비스로 보냄
- 뉴스 피드 서비스 : 캐시에서 뉴스 피드를 가져오는 서비스
- 뉴스 피드 캐시 : 뉴스 피드를 렌더링할 때 필요한 피드 ID를 보관

### 뉴스 피드 API

클라이언트가 서버와 통신하기 위해 사용하는 수단

HTTP 프로토콜 기반

- 피드 발행 API

  새 스토리를 포스팅하기 위한 API

  인자

    - 바디 : 포스팅 내용
    - 인증 헤더 : API 호출을 인증하기 위해 사용
- 피드 읽기 API

  뉴스 피드를 가져오는 API

  인자

    - 인증 헤더 : API 호출을 인증하기 위해 사용

## 상세 설계

### 웹 서버

클라이언트와 통신 및 인증, 처리율 제한 등의 기능 수행

### 포스팅 전송 서비스

팬아웃 이라고도 함

새 포스팅을 그 사용자와 친구 관계에 있는 사용자들에게 전달

- 쓰기 시점 팬아웃

  새로운 포스팅을 기록하는 시점에 뉴스 피드 갱신

  → 포스팅 완료되면 캐시에 포스팅 기록

  장점

    - 뉴스 피드 실시간 갱신 → 친구들에게 즉시 전송
    - 뉴스 피드를 읽는 시간 짧아짐

  단점

    - 친구가 많은 사용자의 경우 피드 갱신에 많은 비용과 시간 소모
    - 서비스를 잘 사용하지 않는 사용자에게도 피드 갱신 → 자원 낭비 가능
- 읽기 시점 팬아웃

  피드를 읽어야 하는 시점에 뉴스 피드를 갱신

  장점

    - 비활성화된 사용자, 로그인하지 않은 사용자의 경우 유리 → 자원 소모 방지
    - 데이터를 친구 각각에게 푸시하는 작업 필요 X → 핫키 문제 방지

  단점

    - 뉴스 피드를 읽는 데 많은 시간 소요

두 가지 방식을 결합하여 **대부분 사용자**에게는 **푸시 모델(쓰기 시점 팬아웃)**을 사용하고, **친구나 팔로워가 많은 경우** 해당 사용자의 포스팅을 필요로 할 때 가져가도록 하는 **풀 모델(읽기 시점 팬아웃)** 사용

- 그래프 데이터베이스에서 친구 ID 목록을 가져옴
- 사용자 정보 캐시에 친구들의 정보를 저장 → 이 중 필요한 경우 필터링 (친한 친구 공개 등)
- 친구 목록과 포스팅 ID를 메시지 큐에 넣음
- 팬아웃 작업 서버가 메시지 큐에서 데이터를 꺼내 뉴스 피드 캐시에 넣음 → <포스팅 ID, 사용자 ID> 매핑 테이블 형태 (포스팅 전체 내용을 저장하는 경우 메모리 요구량이 너무 커짐)
- 최신 피드를 주로 확인하고, 과거 피드를 확인하더라도 캐시에 저장된 범위 내의 과거 피드를 확인하는 경우가 대부분이므로 캐시 미스 발생 확률이 낮음 (스크롤을 계속해 캐싱된 범위를 넘어가면 캐시 미스 발생 가능)

## 피드 읽기 흐름 상세 설계

- 사용자가 뉴스 피드를 읽으려는 요청 보냄
- 로드밸런서가 요청을 웹 서버 중 하나로 보냄
- 웹 서버는 피드를 가져오기 위해 뉴스 피드 서비스 호출
- 뉴스 피드 서비스는 뉴스 피드 캐시에서 포스팅 ID 목록을 가져옴
- 뉴스 피드에 표시할 사용자 이름, 사용자 사진, 포스팅 콘텐츠, 이미지 등을 사용자 캐시와 포스팅 캐시에서 가져와 완전한 뉴스 피드를 만듦
- 생성된 뉴스 피드를 JSON 형태로 클라이언트에게 보냄

## 캐시 구조

뉴스 피드의 핵심 컴포넌트

- 뉴스 피드 : 뉴스 피드의 ID를 보관
- 콘텐츠 : 포스팅 데이터 보관 (인기 콘텐츠는 따로 보관)
- 소셜 그래프 : 사용자 간 관계 정보 보관
- 행동 : 포스팅에 대한 사용자의 행위에 관한 정보 저장 (좋아요, 답글)
- 횟수 : 좋아요 횟수 등의 정보 보관

## 추가적으로 고려할 부분

- 데이터베이스 규모 확장
    - 수직 VS 수평
    - SQL VS NOSQL
    - MASTER-SLAVE 다중화
    - 복제본에 대한 읽기 연산
    - 일관성 모델
    - 샤딩
- 웹 계층 무상태 운영
- 많은 데이터 캐싱할 방법
- 여러 데이터 센터 지원 방법
- 메시지 큐를 활용한 결합도 낮추기
- 핵심 메트릭 모니터링